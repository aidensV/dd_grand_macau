class HistoryModel {
  String? id,no, number, date,name,phone,status,qty,total;

  HistoryModel({
     this.id,
     this.no,
     this.number,
     this.date,
     this.name,
     this.phone,
     this.status,
     this.qty,
     this.total
  
  });

}
